# Contributing Guide

- 팀 작업 방식과 협업 규칙에 관한 문서
- 모든 변경은 회의 -> Notion Kanban -> branch -> pull request -> review 흐름을 기준으로 진행한다.
- 모두가 이미 합의한 내용이나 사소한 변경은 branch 생성부터 시작할 수 있다.

## 작업 흐름

1. 회의에서 필요한 작업과 우선순위를 협의한다.
1. Notion Kanban 보드에 필요한 작업을 정리한다.
    - Notion 주소: https://foam-cause-fa6.notion.site/cac92bba275b82e7b6640141a893ea9a
2. 각 작업에는 목적, 담당자, 완료 기준을 작성한다.
3. 담당자가 정해진 뒤 작업용 branch를 생성한다.
4. branch에서 구현 작업을 진행한다.
5. 작업이 끝나면 pull request를 생성한다.
6. 3명의 approve를 받은 뒤 `master`에 반영한다.

## Notion Kanban 사용 규칙

- 모든 작업은 시작 전에 Kanban 보드에 등록한다.
- task 제목은 무엇을 바꾸는지 알 수 있게 구체적으로 작성한다.
- task 본문에는 배경, 목표, 완료 기준, 관련 파일 또는 노트북을 작성한다.
- 담당자가 정해지기 전에는 branch를 만들지 않는다.

## Branch 규칙

- `master`에는 직접 push하지 않는다.
- branch 이름은 서로의 작업이 겹치지 않게만 작성한다.

## Pull Request 규칙

PR에는 다음 내용을 포함한다.

- 변경 요약
- 관련 Notion task
- 주요 변경 파일
- 실행 또는 검증 결과
- 리뷰어가 특히 봐야 할 부분

코드 변경이 있는 PR은 실행 방법과 결과를 적는다.
노트북이나 실험 결과가 바뀐 PR은 사용한 설정값, 주요 결과, 산출물 위치를 함께 기록한다.
모두가 이미 동의한 내용이나 사소한 변경은 간단히 요약만 작성하거나 생략할 수 있다.

## Review 규칙

- 수정 요청은 어떤 문제가 있고 왜 바꿔야 하는지 구체적으로 남긴다.
- 작성자는 리뷰 코멘트에 반영 여부를 답변한다.
- 3명의 approve가 모이기 전에는 `master`에 merge하지 않는다.

## 커뮤니케이션

- 작업이 막히면 오래 혼자 들고 있지 말고 Kanban task나 팀 채널에 공유한다.
- 큰 구조 변경, 데이터 형식 변경, 평가 지표 변경은 구현 전에 팀과의 합의가 선행되어야 한다.
- 작업 범위가 커지면 PR을 나누어 리뷰 가능한 크기로 유지한다.
